# 2026-08-27

## 菲比博物馆 14 个静态预览页优化美化（完成）

- 依据 `D:\dev\Phoebe\开发文档.md` 视觉系统（#07131E/#0E2230/#D8A447/#F2BF58/#E9E3D8/#94A2AA，Cormorant Garamond + Inter）为全部 13 个 phoebe_museum*.html 注入统一设计系统块（fonts link + style + script，置于 `</body>` 前）。
- 注入内容保持 ASCII（中文用 \uXXXX 转义），PowerShell 用 UTF8Encoding($false) 写入，避免乱码。
- 功能：IntersectionObserver 滚动渐显（.rv/.in，prefers-reduced-motion 降级）、移动端抽屉导航（!important 覆盖原 display:none 媒体规则，`.open` 类切换）、返回顶部按钮、死按钮 toast「已记录：原型演示操作」。
- 首页 phoebe_museum_v1.html：.navlinks 抽屉 + art-card 点击跳转详情页（PM-\d+ → phoebe_museum_detail.html#ID）、.world/.creator 卡片分别跳 timeline_community_detail / creator_detail；用正则删除了失效的 `.menu → .navlinks.mobile-open` 绑定。
- collection 页断点为 800px（其余 900px），追加 `@media(max-width:800px){.links{top:64px}}`。
- 收尾：detail 页 timeline rtab 补 active 类；timeline_human_era 页 hero 后加「← 返回 13 个时代总览 / ALL ERAS」链接。
- 校验：提取全部 23 个内联 script 块 node --check 0 失败（注意本机 NODE_OPTIONS 含 --use-system-ca 会报错，需先 `$env:NODE_OPTIONS=$null`）。
- 13 页均确认 font/令牌色/IO/返回顶部注入完整。

## 媒介分类体系全站落地（7 处，完成）

设计：数据模型 = `medium`（单选形态：插画/AI 创作/漫画/3D/视频/表情包）+ `method`（创作方式：AI/手工/混合，作为跨形态聚合标签）。判定优先级：视频→3D→漫画→表情包→插画。六类徽章色：插画 #d8a447、AI #9b7ede、漫画 #c96f5a、3D #4fa3b8、视频 #e08a3c、表情包 #d98ba3。

- **collection 页**：筛选拆两组 `.filter-row`（媒介 6 tab 带彩色圆点 `i.fd` + 世界观 3 tab）；双变量 `med`×`world`，`update()` 组合判断；hash 直达 `#medium=xxx`（IIFE 解析设置 med）；PM-000009 标签 meme→sticker，其余 meme→comic；「漫画 / 梗图」更名「漫画」。
- **首页**：collection 前插「探索媒介」区（id=mediums），6 张 `.medium-card`（内联 SVG 图标 + --mc 变量色），href 指向 collection#medium=xxx。
- **detail 页**：CREATOR 后加 MEDIUM 行（id=dMedium）；`MEDIUM_MAP`+`mediumKey()` 把 category 映射到媒介键（Digital Art→illustration、AI Creation→ai、Meme→comic、3D→3d、Video→video、特例 PM-000009→sticker）；`mediumBadge()` 生成可点击徽章 + AI 星标。
- **creators 页**：7 位创作者 `.ccard` 加 `.meds` 擅长领域标签（色点+文字，founder=插画+漫画、cyberlab=插画+AI、fabled=插画+3D、strange=漫画+表情包、orbit=视频+3D、nightarch=插画+漫画、mechworks=3D+视频）。
- **creator_detail 页**：hero 加 `.meds`（SPECIALTIES 标签 + 可点击 `.med-tag` 链接到 collection#medium=xxx，@founder=插画+漫画）。
- **submit 页**：CATEGORY select 替换为「MEDIUM 媒介必选单选卡」（`.med-grid` 6 张 `.med-card`，`:has(input:checked)` 高亮）+「CREATION METHOD 创作方式」3 个 `.m-chip`（AI 生成/手工创作/AI+手工混合）；提交校验改查 `input[name=medium]:checked` / `input[name=method]:checked`。
- **admin 页**：artwork-review 列表行 + artwork-mgmt 表格加 MEDIUM 列（`.med-badge` 色点徽章）；两区各加 `.med-filters` 媒介筛选 chips，JS 按 `data-scope`+行 `data-m` 过滤显示。
- **my 页**：MY ARTWORKS 加 `#artworkMeds` 媒介分布统计条（`.ms` chips，数量 0 的置灰 `.off`），点击过滤本区卡片（卡片 `data-m`）；submissions 加 `.med-badge`，artworks/favs 卡片 meta 加色点。
- 校验：改动的 5 页（detail/creators 已在前轮校验）submit/admin/my 的 script 块 node --check 全部通过；关键节点 Select-String 复查 14/14 命中。

技术要点：`:has()` 选择器用于单选卡高亮（现代浏览器 OK）；PowerShell 5.1 内联中文命令会被安全拦截，改用 Write 临时文件注入法。

## 收尾冻结（完成）

用户选择"收尾冻结（推荐）"方向，本次完成：

- **开发文档数据模型对齐原型**：medium（插画/AI创作/漫画/3D/视频/表情包）+ method（AI/手工/混合）写入 §3 信息架构（COLLECTION 删 MEME/COSPLAY/OTHER，加 STICKER）、§12 artworks 字段（category_id→medium+method）、§16 categories 改写为 §16 medium + §16.1 method（含判定优先级与六类徽章色）、§18 submissions 字段、§21 投稿流程、§26 collection 筛选（MEDIUM+METHOD）、§41 关系图。
- **collection 页增强**（搜索/LOAD MORE/空状态此前已有骨架，本次升级）：搜索范围补 creator/world 字段（对齐文档搜索规范）；LOAD MORE 改为分批加载（limit 初始 5，每次 +5，搜索时 limit=Infinity 全量）；空状态加 RESET FILTERS 重置按钮（重置 med/world/query/limit 并同步按钮 active 态）；旧 expanded 逻辑移除。
- **admin 表格窄屏适配**：860px 断点追加 `.panel{overflow-x:auto}.table{min-width:640px}`（6 列 MEDIUM 表此前窄屏会溢出）。
- **移动端走查结论**：14 页均有布局断点（900/860/820/800px 不等）+ 抽屉导航注入，timeline_human 有 820px 块（media 块正则漏匹配 820 导致一度误判）、submit 有 640px med-grid 适配，无需其他修复。
- 校验：全站 14 页 23 个内联 script 块 node --check 0 失败（读取 HTML 必须用 [System.IO.File]::ReadAllText(UTF8)，Get-Content 默认 GBK 会把中文读乱导致误报语法错误）。
